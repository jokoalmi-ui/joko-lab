================================================================================
                         JOKO LAB — INFORME EJECUTIVO
                          5 de agosto de 2026
================================================================================

1.  FECHA Y PROVEEDOR ACTIVO
2.  ESTADO DEL STACK DOCKER
3.  GPU Y RECURSOS
4.  ÚLTIMAS DECISIONES DOCUMENTADAS
5.  ÚLTIMOS COMMITS
6.  CAMBIOS SIGNIFICATIVOS RESPECTO AL INFORME ANTERIOR
7.  PRÓXIMO PASO RECOMENDADO

================================================================================
1. FECHA Y PROVEEDOR ACTIVO
================================================================================

Fecha: 2026-08-05
Proveedor activo: deepseek (deepseek-v4-flash vía https://api.deepseek.com/v1)
  - Confirmado en ~/.hermes/config.yaml: model.provider=deepseek,
    model.default=deepseek-v4-flash, model.base_url=https://api.deepseek.com/v1
  - No aparece gemini como proveedor activo en la configuración actual.
  - Sin cambio de proveedor respecto al informe anterior.

================================================================================
2. ESTADO DEL STACK DOCKER
================================================================================

El stack Docker está OPERATIVO (4 contenedores UP en la captura).

`docker compose -f /home/jokoalmi/automation-stack/docker-compose.yml ps -a`
devuelve 4 contenedores activos (todos "Up 16 seconds", health de
stirling-pdf en "starting"):

  - n8n             UP (127.0.0.1:5678->5678/tcp)
  - ollama          UP (127.0.0.1:11434->11434/tcp)
  - pdf-cleaner     UP (127.0.0.1:8000->8000/tcp)
  - stirling-pdf    UP (127.0.0.1:8081->8080/tcp, health: starting)

Nota: los contenedores llevaban ~16-17 segundos activos en la captura,
coherente con el arranque diario del stack (inicio.sh / docker compose up -d).
Mismo estado que ayer: stack operativo, sin contenedores caídos.

================================================================================
3. GPU Y RECURSOS
================================================================================

GPU: NVIDIA RTX A2000 12GB
  - Lectura (nvidia-smi): 3069 MiB / 12282 MiB VRAM, 50°C, 100% util, 68.49W
  - Procesos GPU (nvidia-smi --query-compute-apps):
    - hermes-rag/bin/python3 (1 proceso, 2806 MiB)
    - ptyxis (44 MiB)
  - Interpretación: la GPU sigue bajo carga activa (100% util, 50°C) pero con
    menos VRAM en uso que ayer (3069 MiB vs 6133 MiB) y un solo proceso
    hermes-rag (ayer aparecían 2 procesos de 2806 MiB cada uno).

RAM (free -h):
  - Total: 30 GiB | Usado: 6,2 GiB | Disponible: 24 GiB | Swap: 8 GiB sin uso

Disco (df -h):
  - / (nvme0n1p2):       468G total, 202G usados, 243G libres (46%)
  - /mnt/ssd_ia_datos:   440G total, 194G usados, 223G libres (47%)
    (Google Drive montado bajo /mnt/ssd_ia_datos/GoogleDrive; el mount de
    gdrive no aparece como punto independiente en esta captura)

================================================================================
4. ÚLTIMAS DECISIONES DOCUMENTADAS
================================================================================

docs/decisiones/ (últimas 5 entradas):

  2026-08-04  deepseek-cloud-permanente.md  ← NUEVA (04/08)
  2026-07-16  evaluacion-glm-4-7-flash.md
  2026-07-16  watchdog-versiones-hermes.md
  2026-07-15  fix-deepseek-disponibilidad.md
  README.md

Observación: hay UNA decisión nueva documentada desde el informe anterior:
"deepseek-cloud-permanente" (04/08). Es la primera decisión nueva desde el
16/07/2026.

================================================================================
5. ÚLTIMOS COMMITS
================================================================================

git log --oneline -5 (hermes-lab):

  88b9663 checkpoint: 2026-08-04 22:50:29 CEST
  2b5ed6c fix(state-manager): restaura SECRETS_DIR; tests DE/state_manager alineados con API real
  4031e3f checkpoint: 2026-08-03 23:11:31 CEST
  77ea6cd docs(informes): informe ejecutivo 2026-08-03
  0f3623a checkpoint: 2026-08-02 23:33:59 CEST

Observación: los checkpoints diarios continúan activos (último el 04/08 a las
22:50). Hay un commit de tipo fix nuevo: 2b5ed6c (state-manager / SECRETS_DIR).

================================================================================
6. CAMBIOS SIGNIFICATIVOS RESPECTO AL INFORME ANTERIOR
================================================================================

Último informe ejecutivo: 2026-08-04 (hace 1 día). Dentro de la ventana de
7 días, pero hay cambios significativos: sí procede generar.

Cambios detectados:

  a) DECISIÓN NUEVA — deepseek-cloud-permanente (04/08).
     Nueva decisión documentada en docs/decisiones/ (la primera desde el
     16/07). Implica mantener deepseek cloud como proveedor permanente.
     No hay cambios en la configuración activa detectados hoy: sigue
     deepseek-v4-flash vía api.deepseek.com.

  b) FIX APLICADO — commit 2b5ed6c (state-manager).
     "fix(state-manager): restaura SECRETS_DIR; tests DE/state_manager
     alineados con API real". Bug corregido en state-manager; los tests
     de DE/state_manager se alinearon con la API real.

  c) DISCO /mnt/ssd_ia_datos: variación grande.
     Ayer: 312G usados (75%). Hoy: 194G usados (47%). ~118 GB menos
     ocupados. Solo lectura de df: no aparece en la salida la causa de la
     reducción (limpieza de modelos/imágenes/logs, o relectura tras cierre).
     Queda como dato a vigilar.

  d) GPU con menos VRAM en uso pero misma carga.
     Ayer: 6133 MiB VRAM, 58°C, 2 procesos hermes-rag (2×2806 MiB).
     Hoy: 3069 MiB VRAM, 50°C, 1 proceso hermes-rag (2806 MiB).
     La util sigue al 100%.

  e) RAM en descenso leve.
     Usada: 9,1 GiB (ayer) → 6,2 GiB (hoy). Disponible: 21 → 24 GiB.

  f) Stack Docker: sin cambios relevantes (operativo, arranque diario).

Pendientes arrastrados (sin evidencia de avance):
  - Métricas de coste real por proveedor (Sprint 3.4, no implementado)
  - Bug #62723 de Hermes v0.17.0 (bloqueo de actualización a v0.18.x)
  - Integración GLM-4.7-Flash (pendiente de evaluación)

================================================================================
7. PRÓXIMO PASO RECOMENDADO
================================================================================

Confirmar el alcance de la decisión deepseek-cloud-permanente (qué implica
para fallback y coste) y verificar el estado de SECRETS_DIR tras el fix
2b5ed6c en el siguiente ciclo de pruebas. A medio plazo, retomar Sprint 3.4
(observabilidad de costes reales), pendiente desde julio.

================================================================================
Fin del informe — generado automáticamente el 2026-08-05
================================================================================
